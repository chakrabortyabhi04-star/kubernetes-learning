apiVersion: v1
kind: Pod
metadata:
  name: memory-hog
spec:
  containers:
    - name: memory-hog
      image: nginx
      resources:
        requests:
          memory: "64Mi"
        limits:
          memory: "128Mi"
